# ===== Secrets / env =====
.env
.env.*
!.env.example
!.env.production

# ===== Python =====
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/
*.log

# ===== Flask / misc =====
instance/
*.sqlite3

# ===== Node / Next =====
node_modules/
.next/
out/
dist/
build/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# TypeScript incremental build cache (not source code)
*.tsbuildinfo

# npm lockfile — project uses pnpm. Prevents lockfile drift if someone runs
# `npm install` by accident.
package-lock.json

# ===== OS / editor =====
.DS_Store
Thumbs.db
.vscode/
.idea/

# ===== Docker / local volumes =====
dbdata/
*.pid

# ===== Local tool binaries====
/trivy.zip
/trivy.exe
/checkov
/*.zip

# ===== Private internal scripts / demo data =====
backend/scripts/_private/

# ===== Private planning docs & Claude Code local settings (not public) =====
# The compliance SUBSYSTEM is public; only the internal planning doc is hidden.
compliance/GOALS.md
.claude/

# ===== Generated compliance evidence (regenerated by run.py every run) =====
# These are "output," not source. Ignoring them stops merge conflicts between
# feature branches and the daily evidence bot. Regenerate locally with:
#   python compliance/run.py
compliance/output/
compliance/dashboard/data.js

# ===== Terraform =====
*.tfstate
*.tfstate.*
*.tfstate.backup

# Local working directory and provider plugins.
.terraform/

# Local override files — used to override variables for local testing only.
terraform.tfvars
*.auto.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration files (may contain credentials)
.terraformrc
terraform.rc

# DO commit .terraform.lock.hcl — locks provider versions for reproducibility.
