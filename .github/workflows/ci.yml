name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Weekly re-scan (Mondays 06:00 UTC) so newly disclosed CVEs in unchanged
    # dependencies are caught even without a code change. Satisfies NIST 800-171
    # 3.11.2: scan periodically AND when new vulnerabilities are identified.
    - cron: "0 6 * * 1"

jobs:
  backend-security:
    name: Backend Security Scan (Bandit + pip-audit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install security tools
        run: pip install bandit pip-audit

      - name: Run Bandit SAST scan
        run: |
          bandit -r backend/ \
            --exclude backend/__pycache__,backend/migrations,backend/scripts \
            --severity-level medium \
            --confidence-level medium \
            -f txt

      - name: Run pip-audit dependency scan
        run: pip-audit -r backend/requirements.txt

  container-scan:
    name: Container Vulnerability Scan (Trivy)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Trivy
        run: |
          wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | gpg --dearmor | sudo tee /usr/share/keyrings/trivy.gpg > /dev/null
          echo "deb [signed-by=/usr/share/keyrings/trivy.gpg] https://aquasecurity.github.io/trivy-repo/deb generic main" | sudo tee /etc/apt/sources.list.d/trivy.list
          sudo apt-get update -qq
          sudo apt-get install -y trivy

      - name: Trivy filesystem scan (dependency vulnerabilities)
        run: |
          trivy fs \
            --scanners vuln \
            --severity HIGH,CRITICAL \
            --exit-code 1 \
            --ignore-unfixed \
            .

      - name: Trivy IaC scan (Terraform misconfigurations)
        run: |
          trivy config \
            --severity HIGH,CRITICAL \
            --exit-code 1 \
            infra/

  iac-compliance-scan:
    name: IaC Compliance Scan (Checkov, HIPAA framework)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Checkov
        run: pip install checkov

      - name: Run Checkov against Terraform
        # --soft-fail-on LOW means low-severity findings don't break the build.
        # HIGH/CRITICAL findings still fail. Adjust as your tolerance evolves.
        run: |
          checkov \
            --directory infra/ \
            --framework terraform \
            --soft-fail-on LOW,MEDIUM \
            --compact \
            --output cli

  frontend-checks:
    name: Frontend Audit & Lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          # Pin to the major version used locally. pnpm bumps minimum Node and
          # changes lockfile hashing across majors, so "latest" causes drift.
          # Bump this in lockstep when you upgrade pnpm locally.
          version: 10

      - uses: actions/setup-node@v4
        with:
          # pnpm 11+ requires Node 22.13+. Pin to current LTS to stay ahead of
          # pnpm's minimum-Node creep.
          node-version: "22"
          cache: "pnpm"
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Dependency vulnerability audit
        run: pnpm audit --audit-level high

      - name: Lint
        run: pnpm run lint
